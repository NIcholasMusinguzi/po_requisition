<odoo>

    <record model="ir.module.category" id="module_category_po_requisition">
        <field name="name">PO Requsition</field>
        <field name="sequence">7</field>
    </record>

    <record id="group_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="category_id" ref="module_category_po_requisition"/>
    </record>

    <record id="group_user" model="res.groups">
        <field name="name">User</field>
        <field name="category_id" ref="module_category_po_requisition"/>
    </record>

    <!-- <record id="requisitions_by user" model="ir.rule">
        <field name="name">View Requisitions by User</field>
        <field name="model_id" ref="model_po_requisition"/>
        <field name="domain_force">['|',('create_uid','=',False),('create_uid','=',user.id)]</field>
        <field name="groups" eval="[(4, ref('group_user'))]"/>
    </record>

    <record id="admin_view_all_requisitions" model="ir.rule">
        <field name="name">View All Requisitions</field>
        <field name="model_id" ref="model_po_requisition"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('group_manager'))]"/>
    </record> -->

</odoo>